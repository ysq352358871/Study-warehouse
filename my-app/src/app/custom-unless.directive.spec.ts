import { CustomUnlessDirective } from './custom-unless.directive';

describe('CustomUnlessDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomUnlessDirective();
    expect(directive).toBeTruthy();
  });
});
