import { MyValidationTwoDirective } from './my-validation-two.directive';

describe('MyValidationTwoDirective', () => {
  it('should create an instance', () => {
    const directive = new MyValidationTwoDirective();
    expect(directive).toBeTruthy();
  });
});
