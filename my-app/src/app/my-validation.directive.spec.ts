import { MyValidationDirective } from './my-validation.directive';

describe('MyValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new MyValidationDirective();
    expect(directive).toBeTruthy();
  });
});
