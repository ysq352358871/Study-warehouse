import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { AccountUpdatePassPage } from './account-update-pass';

@NgModule({
  declarations: [
    AccountUpdatePassPage,
  ],
  imports: [
    IonicPageModule.forChild(AccountUpdatePassPage),
  ],
})
export class AccountUpdatePassPageModule {}
